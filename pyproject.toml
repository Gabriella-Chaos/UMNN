[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "umnn"
version = "0.1.0"
description = "Unified Monotonic Neural Networks with Triton optimization"
readme = "README.md"
authors = [
  { name = "Gabriella Chaos", email = "gabriella-chaos@users.noreply.github.com" },
]
license = { file = "LICENSE" }
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Science/Research",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.8",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Topic :: Scientific/Engineering :: Artificial Intelligence",
]
keywords = ["deep-learning", "pytorch", "monotonic-networks", "triton", "normalizing-flows"]
dependencies = [
    "torch>=2.0.0",
    "numpy",
    "triton; platform_system=='Linux'",
]
requires-python = ">=3.8"

[project.optional-dependencies]
dev = [
    "pytest",
    "pytest-cov",
    "black",
    "isort",
    "flake8",
]

[project.urls]
Homepage = "https://github.com/Gabriella-Chaos/UMNN"
Repository = "https://github.com/Gabriella-Chaos/UMNN.git"
"Bug Tracker" = "https://github.com/Gabriella-Chaos/UMNN/issues"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
minversion = "6.0"
addopts = "-ra -q"
testpaths = [
    "tests",
]

[tool.black]
line-length = 88
target-version = ['py38']
include = '\.pyi?$'

[tool.isort]
profile = "black"
multi_line_output = 3